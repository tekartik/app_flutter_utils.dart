name: tekartik_app_flutter_firebase_firestore
description: Firestore for flutter application
version: 0.1.0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  tekartik_firebase_firestore_browser:
    git:
      url: git://github.com/tekartik/firebase_firestore.dart
      path: firestore_browser
      ref: null_safety
    version: '>=0.4.0'
  tekartik_firebase_firestore_flutter:
    git:
      url: git://github.com/tekartik/firebase_flutter
      path: firestore_flutter
      ref: null_safety
    version: '>=0.4.0'
  tekartik_app_flutter_firebase:
    git:
      url: git://github.com/tekartik/app_flutter_utils.dart
      path: app_firebase
      ref: null_safety
dev_dependencies:
  flutter_test:
    sdk: flutter
  process_run: '>=0.10.3'
  tekartik_app_lints_flutter:
    git:
      url: git://github.com/tekartik/app_flutter_utils.dart
      ref: null_safety
      path: app_lints
    version: '>=0.1.0'

dependency_overrides:
  tekartik_app_flutter_firebase:
    path: ../app_firebase